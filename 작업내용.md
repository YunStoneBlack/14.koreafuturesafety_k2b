# 작업내용

## 2026-09-15 (Day 1)

### 목표
12-1 프로젝트(hwpx버전)에서 생성된 기술지도 결과보고서를 K2B(안전보건공단,
k2b.kosha.or.kr) "재해예방기관 기술지도" 화면에 자동 입력/첨부하는 프로그램 개발 시작.
**최종 "저장"(제출) 버튼은 모든 기능이 검증된 뒤 맨 마지막에 추가한다** — 그 전까지는
입력/첨부까지만 자동화하고 사람이 직접 확인 후 수동으로 저장.

### 진행한 것
- 프로젝트 뼈대 구성: `requirements.txt`(playwright/keyring/PyQt6), `core/config.py`,
  `.gitignore`
- `core/credentials.py` — K2B 로그인 정보를 Windows 자격 증명 관리자(keyring)에
  암호화 저장. `python -m core.credentials set/show/delete` CLI 제공.
- `core/db_reader.py` — 12-1의 `app.db`(SQLite)를 읽기 전용으로 열어 현장명 기준
  회차 데이터를 가져오는 모듈. 실제 테스트 DB로 동작 검증 완료(단, 12-1이 아직 실전
  투입 전이라 현재 DB는 테스트 데이터뿐).
- `desktop/gui_style.py`/`gui_constants.py`/`core.py` — 6/7/8/10/11번 프로젝트의
  라벤더/퍼플 카드형 디자인 시스템(원본: 11번 `app/gui_style.py`)을 그대로 이식,
  목업 화면으로 렌더링 검증 완료.
- **Playwright codegen으로 실제 K2B 화면 흐름 1차 녹화** (`record_flow.py`) →
  `data/recorded_flow.py`(비밀번호 즉시 redaction 처리).
- CDP 디버그 브라우저(`launch_debug_browser.py`, 포트 9333)를 띄워 Claude가 직접
  여러 차례에 걸쳐 화면을 조작/스크린샷하며 셀렉터를 검증하는 방식으로 전환 — 사용자가
  로그인만 직접 하고 이후 탐색은 Claude가 진행.

### 확정된 실제 화면 흐름 (`core/k2b_client.py`, `core/k2b_selectors.py`)
1. 로그인 (`core.credentials`에 저장된 계정)
2. 로그인 직후 대시보드에서 "재해예방기관 기술지도" 카드 **단일 클릭**으로 검색화면 진입
   (기존에 만들었던 "나의업무→건설재해예방전문기관 기술지원→트리메뉴" 3단계 경로보다
   훨씬 단순 — 코드도 이 방식으로 단순화함)
3. 현장명 검색 → **결과 행을 텍스트 클릭으로 선택**(체크박스 불필요, CONFIRMED) → 상세보기
4. "재해예방기술지도 등록" 모달에서 "+ 추가"로 새 차수 생성(기술지도일 오늘 날짜 자동
   채워짐, CONFIRMED) 또는 기존 차수 선택
5. 필드 입력 — 전부 CONFIRMED:
   - 기술지도일: 캘린더 아이콘 클릭 → 월별 팝업 → 날짜 클릭(같은 달 안에서만 검증,
     다른 달로 이동하는 방법은 TODO)
   - 공정률: 텍스트 입력
   - 현재 작업공증: 드롭다운 7종(가설/굴착/골조/마감/부대토목/실내인테리어/기타공사)
   - 비계사용현황: 사용/미사용 라디오(텍스트클릭, 개별 id 없음) + 비계종류(강관비계/
     시스템비계 체크박스, "사용" 선택 시에만 활성화, 다른 모듈 프리픽스에 속함 주의)
   - 현장책임자 등 통보방법: 5종 전부 CONFIRMED(직접전달/등기우편/전자우편/모바일/기타
     = chk01_001~005)
   - 이전 기술지도 이행여부: 3종 전부 CONFIRMED(이행=chk02_1, 불이행=chk02_N,
     해당없음=chk02_0 — Y/N 대칭이 아닌 특이 코드 체계)
6. 대형사고 위험작업: 추가 → 새 행의 "선택" 드롭다운 클릭 2번 → 옵션 5종 중 선택
   (비계 설치/거푸집동바리/흙막이지보공/기타 추락위험/밀폐공간·화재폭발) → CONFIRMED
   (필수 여부는 미검증, 저장을 안 해봐서)
7. 사진첨부 3종(현장전경/현장점검/현장개선) — **실제 파일 업로드까지 end-to-end 검증**
   (`page.expect_file_chooser()` 방식)
8. 보고서 파일첨부(`btn_etcAddTextBoxElement`) — 맨 아래 "보고서" 섹션. 12-1 산출물
   (PDF/hwpx)이 실제로 여기 들어가는 자리. **중요 제약: 기술지도일 이후 7일 초과 시
   보고서 수정 불가**(실화면에 명시된 안내문) — 자동 제출은 이 기한 안에 실행돼야 함.
9. 저장(=제출) — **의도적으로 아직 미구현**

### 검증 방법 노트
- 실제 등록된 현장("마산3리 공원 조성사업", 사업장관리번호 21681507066)의 기존 2회차
  데이터로 필드 매핑을 검증 — 저장 버튼을 한 번도 누르지 않아 실제 DB는 변경되지 않음.
  날짜/사진/대형사고위험작업 등을 바꿔봤다가 매번 원래 값으로 복원함.
- 로그인 입력 시 `.fill()`을 쓰면 Nexacro 위젯이 기존 값 뒤에 이어붙이는 버그가 있어
  클릭→전체선택→삭제→`press_sequentially()`(실제 타이핑처럼) 방식으로 전환함
  (`K2BClient._type_into`).

### 남은 TODO
- 기술지도일을 **다른 달**로 바꾸는 방법(월 이동 화살표 셀렉터 미검증)
- 지도건수/경영책임자·건설업체 통보일/건설공사 발주자 통보일자 — 12-1 DB에 대응 값
  없음, 사람이 직접 입력해야 하는 필드로 남겨둠(추측 금지)
- 대형사고 위험작업이 필수인지, "해당없음"으로 건너뛸 수 있는지
- 검색 결과가 **여러 건**일 때(동명 현장 등) 행 선택이 여전히 안정적인지 재검증
- 보고서 파일첨부 후 실제 저장까지 이어지는 마지막 단계(README 원칙상 맨 마지막에 추가)
- 12-1 프로젝트가 실전 투입되면 `db_reader.py`를 실제 데이터로 재검증

### 실제 데스크톱 앱 완성 + 실사용 화면에서 end-to-end 자동입력 성공
같은 날 이어서 GUI까지 실제로 연결해 완성함:
- `desktop/worker.py` — Playwright 자동화를 QThread 백그라운드에서 실행(UI 안 멈춤)
- `desktop/main_window.py` — 현장/회차 선택 → 12-1 DB 조회 → 미리보기 → "K2B 화면에
  자동 입력하기" 버튼으로 실제 자동화 실행. 11번 프로젝트 스타일 그대로 사용.
- `desktop/main.py` — 진입점(`python -m desktop.main`)
- `core/db_reader.py`에 `list_sites`/`list_rounds` 추가, 보고서 파일 경로를
  `report_pdf_path`/`report_hwpx_path`로 분리(기존엔 `hwp_path`를 썼는데 이 컬럼은
  12-1 모델 주석상 "예전 DOCX→HWPX 변환 파이프라인용(미사용)" legacy 필드였음을 발견해
  `hwpx_path`로 교체). **K2B에 PDF/HWPX 중 어느 형식을 올려야 하는지는 아직 고객 확인
  전(TODO)** — 지금은 임시로 PDF를 우선 쓰되 화면에 "미확정" 경고를 표시함.

**실제 K2B 사이트(태봉근린공원 현장, 기존 1~3차 등록되어 있던 실사용 현장)에서 앱을
직접 실행해 "차수 4" 신규 추가 → 기술지도일/점검자/공정률/현재작업공정/통보방법/
이전기술지도 이행여부까지 자동 입력되는 것을 실화면으로 확인함.** 저장 버튼은
여전히 누르지 않음(원칙 유지).

발견한 것: "+ 추가" 클릭 시 지도건수가 "2"로 자동 채워졌는데, 이건 우리 코드가 채운 게
아님(`db_reader`/`worker` 어디에도 지도건수를 다루는 로직 없음) — K2B 자체가 "+ 추가"
시 기술지도일(오늘 날짜)뿐 아니라 지도건수 같은 다른 필드도 **직전 차수 값을 기본값으로
복사**해주는 것으로 추정됨(TODO: 다른 필드도 이런 식으로 자동 복사되는지 추가 확인).

### 사용자가 보고서 기준으로 필드 매핑 직접 지정 (실사용 화면 walkthrough)
사용자가 실제 K2B 화면(디버그 브라우저)과 12-1 산출물 PDF를 나란히 보면서 항목별 매핑을
하나씩 불러줌. 확정된 매핑:
- 기술지도일 ← 보고서 "기술지도실시일"
- 공정률 ← 보고서 "공정율"
- 차수 ← 보고서 "회차: 총 N회차 중 (M)회"의 M
- 점검자 ← 보고서 "담당요원" **텍스트**(옆의 서명 이미지와 혼동 금지) — 단, 아래 발견에 따라 실제로는 K2B에서 못 채움
- 현장책임자명/연락처 ← 보고서 "성명"/"연락처". 연락처는 K2B 입력창이 3칸(지역-국번-번호)
  분리 구조라 숫자만 추출해서 나눠 넣어야 함(보고서 표기가 "031-757-1930"/"0317571930"
  두 방식 다 나올 수 있음 — 숫자만 뽑는 정규화 필요)
- 비계사용현황 ← 기본 "미사용", 단 보고서 "6. 필수 지도 확인 사항"의 12대 기인물 표에서
  "④ 비계작업발판"이 체크(☑)되어 있으면 "사용"
- 이전 기술지도 이행여부 ← 보고서 "4. 이전 기술지도 사항 이행여부"에서 체크된 항목
  (이행/일부이행/불이행) — 단 K2B엔 "일부이행"에 대응하는 옵션이 없어 보임(TODO: 매핑 방법 확인 필요)
- 현장전경사진/현장점검사진 ← 보고서 "3. 전경사진 및 점검사진"에 들어간 원본 사진 파일
  그대로(=12-1 DB의 overview_photo_paths/inspection_photo_paths, 이미 연결돼 있음)
- 현장개선사진 ← 보고서 "4. 이전 기술지도 사항 이행여부"의 "이행결과" 칸에 사진이 첨부된
  경우 그 사진(=12-1의 PreviousFinding 이행완료 증빙사진 쪽, 아직 db_reader에 연결 안 함, TODO)
- 문제점 및 개선 요청사항 ← 보고서 "8. 현재 공정 내 현존하는 위험성 제거" 표의
  "유해위험요인" 텍스트 + " / " + "지적사항 재해예방대책" 텍스트를 합친 것
  (예: "정화조 청소시 유해가스로 인한 재해 / 마스크 착용 권고")
- 불량사업장 통보 / 대형사고 위험작업 ← 둘 다 별도 선택 UI 필요(자동 매핑 없음), 단
  대형사고 위험작업은 기본값이 "해당없음" 체크 상태라 대부분의 경우 그대로 둬도 됨
- **보고서 파일 형식** ← 사용자가 직접 K2B에서 HWP/HWPX 첨부를 시도했더니 **첨부 자체가
  안 되고 PDF만 가능**함을 확인 → "PDF vs HWPX 고객 확인 필요" TODO 해소, PDF로 확정
  (`db_reader.py`의 `report_file_path`가 이제 항상 `report_pdf_path`를 씀)

### 저장 이후 삭제 가능 여부 검증 (안전한 테스트를 위해 중요)
사용자 질문("저장해도 삭제할 수 있나?")에 답하기 위해 실제로 테스트: "+추가"로 테스트
회차(현장책임자명="테스트책임자", 특이사항="TEST-삭제예정" 등 식별 가능한 값) 생성 →
사용자가 직접 저장 버튼 클릭 → Claude가 디버그 브라우저에서 조회정보로 저장 확인 →
차수/현장책임자명 값을 다시 읽어 안전 확인한 뒤 "삭제" 버튼 클릭 → "정상적으로
삭제되었습니다" 확인. **결론: 저장 후에도 삭제로 완전히 되돌릴 수 있음(CONFIRMED)** —
앞으로 테스트 시 실수로 저장해도 삭제로 정리 가능.

### 새로 확보한 필드 셀렉터 (DOM id 전체 스캔으로 확인)
- 현장책임자명: `edt_SPT_RSPNBER_NM_input` — CONFIRMED 편집 가능
- 특이사항: `edt_PARTCLR_MATTER_CN_input` — CONFIRMED 편집 가능
- 지도건수/교육인원/배포자료건수: `edt_CCH_NOCS_input`/`edt_PREARNGE_NMPR_CNT_input`/
  `edt_WDTB_DATA_NOCS_input` — id만 확보, 편집 가능 여부 미검증
- **점검자**: `edt_INSCTR_NM_input` — **CONFIRMED readonly**. 실제로 값을 바꿔보려
  시도했으나 `is_editable()=False`, `readonly` 속성 존재, 입력해도 값이 안 바뀜.
  기존 차수마다 점검자 값이 다른 이유(예: 차수3=현우선, 차수1/2=현민재)는 그 회차를
  저장할 당시 로그인했던 계정이 달랐기 때문으로 추정 — **담당요원별로 K2B 계정이 별도로
  있는지 고객 확인 필요(보류 중)**. 확인 전까지 이 필드는 자동입력 로직에서 제외.
- **현장책임자 연락처**: `msk_SPT_RSPNBER_TELNO_input` — CONFIRMED. 겉보기엔 3칸
  (지역-국번-번호)처럼 보였지만 실제론 **마스크 입력 단일 필드 하나**였다(빈 값일 때
  placeholder "___-____-____"가 3칸처럼 보인 것). 숫자만 순서대로 입력하면
  "010-9999-8888"처럼 자동으로 하이픈 포맷됨(실기록: "01099998888" 입력 → 정확히 반영,
  원복 완료). `K2BClient.fill_site_manager_phone()`이 보고서 표기(하이픈 有/無 둘 다)에서
  숫자만 추출해 처리하도록 구현하고 `worker.py`에 연결함.

### 로그인 계정 관리 화면 (완료)
점검자가 로그인 계정에 고정된다는 걸 감안해, 담당요원별로 여러 K2B 계정을 등록/선택할
수 있게 구현함:
- `core/credentials.py` — 단일 계정 저장 구조를 "담당요원 이름 -> (K2B 아이디, 비밀번호)"
  여러 건 저장하는 구조로 확장(`list_accounts`/`set_account`/`get_account`/
  `delete_account`, keyring에 이름 인덱스를 JSON으로 별도 저장). 기존에 저장돼 있던
  단일 계정은 실제 로그인 화면에 표시된 이름("현민재")으로 자동 마이그레이션함.
- `desktop/account_dialog.py`(신규) — 계정 목록 조회/추가/삭제 다이얼로그.
- `desktop/main_window.py` — 상단에 "K2B 로그인 계정" 콤보박스 + "계정 관리" 버튼 추가.
  데이터를 불러오면 `inspector_name`과 이름이 같은 등록 계정을 자동 선택하고, 없으면
  경고 로그를 띄움(실제 화면 렌더링으로 두 경우 다 확인함).
- `run_test_fill.py`도 새 다계정 구조에 맞게 인자(현장명, 담당요원이름)를 받도록 수정.
- 담당요원별 K2B 계정이 실제로 있는지 자체는 여전히 고객 확인 전(보류 중) — UI/저장
  구조는 계정이 하나뿐이어도 정상 동작하므로 확인 결과와 무관하게 먼저 구현함.

### 스크롤 버그 수정 + git 저장소 생성/push
데스크톱 앱을 실제로 다시 실행해서 자동입력을 끝까지 돌려봤더니, 필드 입력은 전부 성공하고
"보고서 파일첨부" 단계에서 `Timeout 30000ms exceeded while waiting for event "filechooser"`
오류 발생 — 저장은 시도되지 않아 실제 데이터는 안전했음.

원인: 이 사이트(Nexacro)는 상세 모달 전체가 `overflow:scroll` 컨테이너가 아니라
절대좌표로 배치된 하나의 거대한 패널이라, Playwright의 `.click()`이 자동으로 시도하는
`scrollIntoViewIfNeeded`가 먹히지 않는다. 화면 아래쪽(보고서 파일첨부 버튼 등)은 실제
마우스 휠 이벤트로 스크롤해야 클릭 가능하다(디버그 브라우저로 이미 알고 있던 사실인데,
`k2b_client.py` 자동화 코드에는 반영이 안 돼 있었음).

수정: `K2BClient._scroll_into_view(locator)` 헬퍼 추가 — `bounding_box()`로 대상이
뷰포트 안(40px 안전마진 포함)에 있는지 확인하고, 아니면 실제 `mouse.wheel()`로 반복
스크롤한 뒤 재확인한다. `_type_into`, `attach_photos`, `attach_report_file`,
`select_current_process`, `check_notification_method`,
`check_prev_guidance_implemented`, `set_scaffold_usage`, `add_major_hazard_work`
전부에 적용해 어떤 필드를 클릭/입력하기 전이든 안전하게 스크롤되도록 함.

파일 구조(`desktop/worker.py`/`main.py`/`main_window.py`/`account_dialog.py`)는 이미
용도별로 잘 분리돼 있어 추가로 쪼개지 않음(가장 큰 파일도 227줄, 12-1의 리팩토링
기준인 600줄에 한참 못 미침). 문법/import 전체 재검증 후, 새로 만든 GitHub 저장소
(`git@github.com:YunStoneBlack/14.koreafuturesafety_k2b.git`)에 최초 커밋 + push 완료.

### 진짜 원인은 스크롤이 아니라 "기존 차수 편집 + 수정기한 만료"였음
스크롤 수정 후 앱을 재실행해서 다시 돌렸더니 **똑같은 filechooser 타임아웃이 재현됨**.
사용자가 먼저 알아챈 것: "K2B에 이 회차가 아직 없음(새 차수로 추가)" 체크박스가 꺼져
있어서, "+추가"를 안 누르고 **K2B에 우연히 열려있던 기존 차수(3차)를 그대로 덮어쓰는**
동작이었다. 그런데 12-1의 회차 번호(2회차)와 K2B의 차수 번호(1~3차)는 서로 완전히
무관한 별개의 카운터라서, 체크박스를 꺼두면 엉뚱한 기존 데이터를 덮어쓸 위험이 있다.

디버그 브라우저로 동일 뷰포트(1280x720, `open_browser()`와 동일)에서 직접 재현한 결과:
- 스크롤 자체는 정상 동작함(`_scroll_into_view`가 y=1553(화면 밖) → y=593(화면 안)으로
  실제로 스크롤시킴, 스크린샷으로 파일첨부 버튼이 눈에 보이는 것까지 확인)
- 그런데도 클릭 후 file chooser가 안 뜸 → 스크린샷을 자세히 보니 **"파일첨부" 버튼
  자체가 비활성화(회색)** 상태였음. 옆에 "보고서 수정가능 기한: 2026-09-02"라고
  표시돼 있었는데, 이 기존 3차의 기술지도일(2026-08-26)+7일 = 2026-09-02이고 오늘
  (2026-09-15)은 이미 그 기한을 13일 지남 — 그래서 버튼이 막혀 있었던 것.

즉 스크롤 수정은 옳았지만 실제 오류의 진짜 원인이 아니었다. 진짜 원인은 "기존 차수를
그대로 편집하면 보고서 수정기한이 지나 있을 수 있다"였음.

수정한 것:
- `desktop/main_window.py` — "새 차수로 추가" 체크박스를 **기본값 체크**로 변경(꺼두면
  기존 데이터를 덮어쓸 위험 + 기한 만료 위험이 있어서, 의도적으로 기존 차수를 고칠
  때만 사람이 직접 꺼야 하도록).
- `core/k2b_selectors.py`에 `REPORT_MODIFIABLE_UNTIL_INPUT_ID`
  (`cal_MDFCN_POSBL_YMD_calendaredit_input`, "수정가능일" 필드) 추가.
- `core/k2b_client.py`에 `ReportModificationExpiredError` 예외 추가 — 보고서 파일첨부
  시도 전에 이 기한 필드를 먼저 읽어 오늘 날짜와 비교하고, 지났으면 "Timeout waiting
  for filechooser"라는 모호한 오류 대신 "보고서 수정가능 기한(YYYY-MM-DD)이 지나
  파일첨부 버튼이 비활성화되어 있습니다. 새 차수로 추가하세요" 같은 명확한 안내를
  즉시 띄운다.

### exe 배포판 빌드 (고객 데모용, "아직 완벽하진 않지만 구현된 것만 보여주기")
12-1처럼 PyInstaller로 고객이 바로 실행할 수 있는 exe를 만들었다. 두 가지 설계 결정:

1. **Playwright 번들 Chromium 대신 고객 PC의 기존 구글 크롬을 그대로 제어**
   (`core/k2b_client.py`의 `_find_system_chrome()` — Program Files/Program Files (x86)/
   LOCALAPPDATA 하위 기본 설치 경로를 순서대로 확인, `chromium.launch(executable_path=...)`
   로 그 크롬을 켠다). Chromium 바이너리(150MB+)를 배포판에 안 넣어도 되고, 대부분의
   PC에 크롬이 이미 있어서 별도 설치 요구가 거의 없음. 시스템 크롬을 못 찾으면 개발
   환경처럼 Playwright 번들 Chromium으로 자동 폴백.

2. **"불러오기"가 exe만 받아도 바로 동작하도록 데모 DB를 함께 배포** — 사용자가 "앱에서
   불러오기했던 그 보고서까지 같이 보내자"고 요청. 문제는 12-1의 실제 app.db가 파일
   경로를 이 개발 PC의 절대경로로 저장하고 있어(`C:\Users\...\report_6.pdf`) 그대로
   옮기면 깨진다는 것. 해결:
   - `packaging/build_demo_data.py`(신규) — 12-1 app.db를 `data/demo/app.db`로 복사하고,
     그 DB가 가리키는 보고서 PDF 실물도 `data/demo/`에 같이 복사한 뒤, DB 안의
     `pdf_path`를 **파일명만 남긴 상대경로**로 고쳐씀(사용자가 먼저 제안: "상대경로로
     해야겠지?").
   - `core/db_reader.py`에 `_resolve_path()` 추가 — 저장된 경로가 절대경로면 그대로,
     상대경로면 "DB 파일이 있는 폴더 기준"으로 다시 풀어준다. 개발 중인 12-1 DB(절대
     경로)와 배포용 데모 DB(상대경로) 둘 다 이 함수 하나로 커버됨.
   - `core/config.py` — `sys.frozen`이면(exe로 실행 중) exe 옆의 `data/app.db`를,
     아니면(개발 중) 기존처럼 12-1 프로젝트의 실제 DB를 쓰도록 분기.

3. **`packaging/build_exe.py`(신규)** — 12-1의 `build_exe.py` 패턴을 따르되 PyQt6 전용
   (hwpx/pyhwpx 관련 `--add-data` 불필요), 배포 폴더는 **바탕화면**에 직접 생성(사용자
   요청 — 찾아서 전달하기 쉽게), 빌드 후 `data/demo/`를 `dist/<앱이름>/data/`로 복사.

빌드/실행 중 발견하고 고친 버그:
- **PyInstaller 빌드 exe가 즉시 크래시** — `desktop/main.py`가 `from .main_window import
  MainWindow`(상대 임포트)를 썼는데, PyInstaller는 진입점 스크립트를 패키지가 아닌
  최상위 스크립트로 실행해서 "attempted relative import with no known parent package"로
  실패함(사용자가 실제 exe를 실행해보고 스크린샷으로 오류를 보여줘서 발견 — 마침 나도
  동시에 재현하고 있었음). `from desktop.main_window import MainWindow`(절대 임포트)로
  변경, `-m desktop.main`으로 돌리는 개발 환경에서도 그대로 동작하는 걸 확인.

결과: 바탕화면 `한국미래안전_K2B자동제출\` 폴더(exe + data/)를 실제로 실행해서 "불러오기"
로 데모 데이터(태봉근린공원 조성사업)가 정상 표시되는 것까지 확인 완료(사용자 확인:
"응 잘돼").

### 고객 PC에서 실행 실패 → 원인 파악 → 크롬 설치로 해결
사용자가 이 배포 폴더를 실제 고객(다른 PC, 사용자명 "gusal")에게 전달해 실행해봤더니
오류 발생: `Executable doesn't exist at ...\_internal\playwright\driver\package\
.local-browsers\chromium-1234\chrome-win64\chrome.exe` + "Looks like Playwright was
just installed or updated. Please run: playwright install".

원인: `_find_system_chrome()`이 표준 경로(Program Files/Program Files (x86)/
LOCALAPPDATA)에서 크롬을 못 찾음 → Playwright 기본 브라우저 탐색으로 폴백 → 그런데
그 경로에 브라우저 자체가 없음(배포판에 안 넣어뒀으므로). 알고보니 **그 고객 PC에
크롬이 실제로 설치돼 있지 않았음**(사용자 확인: "상대방이 크롬을 깔고있긴 해") — 즉
코드는 정상 동작했고(크롬이 없으니 못 찾은 게 맞음), 그 다음 폴백(Playwright 번들
브라우저)이 배포판에 없어서 막혔던 것.

대응 시도: `PLAYWRIGHT_BROWSERS_PATH=0` 설정 후 `playwright install chromium`으로
Playwright 패키지 자체 폴더(`.local-browsers`) 안에 크로미움을 설치하면 PyInstaller가
자동으로 통째로 수집해 exe에 번들할 수 있다는 걸 확인(재빌드해서 실제로 `chromium-1234`
가 exe 옆에 포함되는 것까지 검증). 다만 이러면 배포 폴더가 **630MB로 커짐**(Chromium
자체가 428MB, 안 쓰는 chromium_headless_shell/ffmpeg/winldd까지 같이 받아져서 정리
필요) — 사용자에게 "가벼운 버전 vs 번들 포함 버전" 트레이드오프를 물어봄.

결론: **고객이 크롬을 설치하니 기존 가벼운 버전(exe 몇 MB)이 바로 정상 동작**함을
확인(사용자: "이미 이전버전 사용하고 있어 크롬 깔고 다시 하니까 잘된대"). 그래서 무거운
번들 버전은 지금은 필요 없다고 판단해 삭제하고, 가벼운 버전(시스템 크롬 의존)을 기본
전략으로 유지하기로 함 — `_find_system_chrome()` 코드는 그대로 두되(대부분의 고객 PC엔
크롬이 있을 것), 크롬이 아예 없는 경우를 위한 번들 대안이 필요하면 언제든
`PLAYWRIGHT_BROWSERS_PATH=0` + `playwright install chromium` 조합으로 다시 만들 수
있다는 것만 기록해둔다(재현 방법 검증 완료).

## 2026-09-16 (Day 2)

### 목표
기술지도일을 "다른 달"로 바꾸는 캘린더 월 이동 기능 구현(Day1 TODO).

### 훨씬 더 심각한 버그를 먼저 발견함: 상세 모달 모듈코드가 세션마다 바뀜
디버그 브라우저로 캘린더 팝업 셀렉터를 찾으려고 DOM을 스캔하다가, 오늘 세션의 상세 모달
모듈코드가 `EBP00120_P01`인데 `core/k2b_selectors.py`엔 어제 확인한 `EBP00130_P01`이
하드코딩돼 있는 걸 발견. 실제로 어제 CONFIRMED로 표시했던 셀렉터들(`PROGRESS_RATE_INPUT_ID`
등)을 오늘 세션에서 `page.locator(...).count()`로 찍어보니 전부 **0건**이었음(진짜로
깨져 있었음, 추측 아님).

원인 추정: `EBP00130_P01`/`EBP00120_P01`처럼 생긴 모듈코드는 고정된 화면 코드가 아니라
Nexacro가 로그인 세션마다 동적으로 배정하는 프레임 인스턴스 번호로 보임. 반면 목록화면의
`103015010`은 오늘도 그대로였음(실측 확인) — 이쪽은 진짜 고정 화면코드. 즉 어제 실전
테스트가 성공했던 건 그날 세션이 우연히 `EBP00130_P01`이었기 때문이고, 세션마다 바뀌는
구조라 **실제 고객 사용 시 날짜에 따라 전체 자동입력이 통째로 실패할 수 있는 잠재
버그**였음.

수정: `k2b_selectors.py`에 `_modal(id_suffix)` 헬퍼 추가 — 상세 모달 관련 모든 상수를
전체 id 하드코딩 대신 CSS attribute selector(`[id$='...']`, id 끝부분 일치)로 바꿔
모듈코드 숫자가 뭐든 매치되게 함. 목록화면(103015010) 쪽 셀렉터는 고정임을 확인했으므로
그대로 둠. 오늘 세션(`EBP00120_P01`)에서 캘린더 버튼/진행률 입력 등 전부 정상 매치되는
것까지 실측 확인.

### 기술지도일 월 이동 구현 (CONFIRMED, 다른 달 포함 실화면 검증)
캘린더 팝업 DOM을 전체 스캔해서 새로 확인한 것(전부 CONFIRMED):
- 팝업 헤더에 연도(`..._popupcalendar_header_yearStatic`, 텍스트 "YYYY.")/월
  (`..._popupcalendar_header_monthStatic`, 텍스트 "MM") 각각 별도 요소로 존재
- 이전달/다음달 버튼: `..._popupcalendar_header_prevbutton`/`_nextbutton`
- **중요 발견**: 날짜 셀 42개(6주 x 7일)가 전부 **동일한 id를 공유**함(Nexacro 특성상
  중복 id) — 텍스트로 찾으면 이전달/다음달 잔여일과 겹쳐 모호함. 심지어 이번 달 마지막
  날이 30/31이면 그리드 맨 앞의 "지난달 30/31" 잔여일과 텍스트가 같아서, 어제 방식
  (`get_by_text(day).first`)은 이 경우 엉뚱한(지난달) 셀을 클릭했을 것 — 어제는 09-09→
  09-20처럼 겹치지 않는 날짜만 테스트해서 못 잡아낸 잠재 버그였음.

해결: `K2BClient.set_guidance_date(target: date)`로 시그니처 변경(기존엔 `day: int`만
받아 같은 달 안에서만 동작). 캘린더를 열어 헤더의 연/월을 읽고, 목표 연월과의 차이만큼
이전/다음 버튼을 클릭해 이동 → 이동 후 헤더가 실제로 목표 연월과 일치하는지 재확인(안
맞으면 `RuntimeError`) → 목표 월 1일의 요일을 `datetime.date(...).weekday()`로 계산해
그리드에서 몇 번째 칸(0-based, 일=0 기준)인지 구해 `.nth(index)`로 정확한 셀을 클릭.

실화면 검증(디버그 브라우저, 태봉근린공원 현장의 저장 안 한 테스트용 "차수 4"에서 진행,
저장은 안 눌러서 실제 데이터엔 영향 없음):
- 2026-09-16(오늘) → 2026-08-05(이전달) → 2026-11-20(다음달로 3개월 이동) →
  2027-01-31(연도 경계 + 월말 31일 엣지케이스) → 2026-09-16(원복) 순서로 전부 스크린샷
  대조하며 정확히 반영되는 것 확인.

`desktop/worker.py`에도 연결 — 그동안 `set_guidance_date`가 어디서도 호출되지 않아
기술지도일은 항상 "+추가"가 채워주는 오늘 날짜 그대로 방치돼 있었음(Day1에 매핑만
확정하고 구현은 누락돼 있었음). `d.guidance_date`(보고서 "기술지도실시일")가 있으면
`datetime.date.fromisoformat()`으로 파싱해 `set_guidance_date`를 호출하도록 추가.

### 남은 TODO
- 대형사고 위험작업 필수 여부 미검증
- 검색 결과 여러 건일 때 행 선택 안정성 재검증
- 점검자 계정별 분리 여부 고객 확인(보류)
- "일부이행" K2B 매핑 방법
- 현장개선사진(이행결과 첨부사진) db_reader 연결
- 최종 "저장" 자동화(의도적 미구현)

### 앱을 실제로 띄워 사용자가 화면 대조 → 6가지 수정/추가 요청 (실사용 리뷰)
사용자가 데스크톱 앱을 직접 돌려보고 K2B 실제 화면과 12-1 보고서 PDF를 나란히 비교하면서
발견한 문제 6가지를 순서대로 지시받아 전부 구현/수정함.

**1. "이전 기술지도 이행여부" 진짜 버그 발견 및 수정** — 보고서엔 "불이행"이 체크돼 있는데
K2B 화면엔 반영이 안 됨. 디버그 브라우저로 DOM을 직접 스캔해 원인 확인: Day1에
`chk02_N="불이행"`/`chk02_0="해당없음"`으로 기록했는데 **실제로는 반대**였음
(`chk02_0`의 실제 textContent="불이행", `chk02_N`의 실제 textContent="해당없음"). 이
때문에 "불이행"을 넣으면 실제로는 "해당없음" 체크박스가 클릭되고 있었음(사용자가 화면에서
직접 목격). `PREV_GUIDANCE_IMPLEMENTED_CHECKBOX_IDS`에서 두 값을 맞바꿔 수정, 실화면에서
"불이행" 체크 정상 반영 확인.

**2. 현재 작업공종** — 보고서와 자동 연동이 신뢰할 수 없어(용어가 K2B 7종 드롭다운과 항상
일치한다는 보장 없음) 앱에 수동 선택 드롭다운 추가(`main_window.py` "K2B 전용 항목" 카드).
"불러오기" 시 12-1 DB 값이 우연히 7종 중 하나와 일치하면 미리 선택해주되, 최종 값은 항상
이 드롭다운 것을 씀(`_apply_manual_fields()`).

**3. 비계사용현황/비계종류** — 마찬가지로 보고서 연동 대신 앱에 수동 선택(사용/미사용
드롭다운 + 강관비계/시스템비계 체크박스, 사용 선택 시에만 활성화) 추가. 클라이언트 메서드
`set_scaffold_usage()`는 Day1에 이미 구현돼 있었는데 `worker.py`에 연결이 안 돼 있던 걸
발견해 이번에 연결함.

**4. 불량사업장 통보 — 신규 기능 (앱 UI + K2B 연동 전부 신규 구현)**
- 앱: 체크박스 + 신고내용 입력 + 드래그앤드롭/파일선택 첨부(`desktop/bad_site_drop_zone.py`
  신규, 12-1의 `pdf_drop_zone.py` 패턴 재사용, 확장자 jpg/jpeg/gif/png/bmp/pdf만 허용)
- K2B 쪽 셀렉터 DOM 전체 스캔으로 확인(전부 CONFIRMED): 체크박스 `Chk_Bnotice`, 신고내용
  `Txa_Notice`(실제 `<textarea>`), 첨부버튼 `btn_errAdd`
- **까다로웠던 부분**: 신고내용 `<textarea>`가 항상 `visibility:hidden`으로 보고됨(체크
  여부와 무관 — Nexacro가 이 영역을 가상 스크롤로 그리는 것으로 추정). 일반 click()/fill()이
  "element is not visible"로 거부해서, 바로 바깥의 보이는 컨테이너를 클릭해 포커스만 준 뒤
  `page.keyboard.type()`으로 입력하는 방식으로 우회(CONFIRMED, textarea.value에 정상
  반영되는 것까지 실측 확인).
- `K2BClient.notify_bad_site()` 신규 구현, `worker.py` 연결. 실제 PDF 파일(3.5MB)까지
  업로드해 "파일 전송 상태 72%" 진행 표시와 최종 파일목록 등록까지 end-to-end 검증.

**5. 대형사고 위험작업 — 신규 기능 (앱 표 UI + K2B 연동 전부 신규 구현), 오늘 가장 오래 걸린 부분**
- 앱: 추가/제거 가능한 표(발생형태/대형사고위험작업/시작일/종료일), 업무영역은 K2B가
  "재해예방 기술지도" 고정값을 자동으로 채워서 다루지 않음
- 그리드 구조를 DOM 전체 스캔으로 재조사(전부 CONFIRMED, 2026-09-16): 행 id 패턴
  `..._grd_List_body_gridrow_{행번호}_cell_{행번호}_{열번호}`(0=순번 1=업무영역 2=발생형태
  3=대형사고위험작업 4=예정시기시작 5=예정시기종료). 발생형태 옵션 5종(전체/붕괴/도괴/
  낙하/질식)을 드롭다운 직접 클릭해 CONFIRMED로 승격(Day1엔 스크린샷 근거 GUESS였음).
  예정시기 캘린더는 기술지도일 캘린더와 완전히 동일한 구조라 `_pick_date_in_open_calendar()`
  공용 헬퍼로 분리해 재사용.
- **실사용 중 발견한 함정 4가지**(전부 CONFIRMED, `k2b_client.py`에 상세 주석):
  1) 새 행 인덱스를 "추가 전 행 개수"로 계산하면 안 됨 — 이 그리드가 화면 밖 행을 DOM에서
     지우는 가상 스크롤을 써서 개수가 실제보다 적게 나옴. "추가 직후 DOM에 남은 행 중 최대
     인덱스"로 계산하도록 수정.
  2) 발생형태(열2)/대형사고위험작업(열3) 콤보는 그리드 전체가 공유하는 오버레이 하나뿐이고,
     Nexacro가 셀 클릭마다 새로 만들고 이전 것을 안 지워 같은 id가 여러 개 쌓임 → 매번
     `.last`로 선택.
  3) 콤보를 고른 직후 바로 다른 열 셀을 클릭하면 이전 콤보가 그 자리에 남아 클릭을
     가로챔 — 같은 행의 업무영역(고정 텍스트) 칸을 블러용으로 클릭하고, 그래도 안 풀리는
     경우(발생형태→위험작업 전환, 시작일→종료일 전환)는 `force=True`로 실제 DOM 노드에
     직접 이벤트를 보내 우회.
  4) **가장 까다로웠던 함정**: 시작일을 먼저 오늘(기본값)보다 뒤로 바꾸면 그 순간 K2B가
     "시작일이 종료일보다 클 수 없습니다" 경고창을 띄우며 막힘(종료일이 아직 오늘 그대로라
     검증 실패). **종료일을 먼저 바꾸고 시작일을 나중에 바꾸면** 항상 안전(목표 종료일은
     정의상 목표 시작일 이후이므로 오늘 날짜인 시작일과 비교해도 안 걸림) — 순서를 뒤집어
     해결.
  - 대형사고위험작업 드롭다운이 한때 옵션 2개만 보여서(가상 스크롤 버그로 의심) 한참
    헤맸는데, 알고보니 **테스트로 한 라운드에 8개 행을 누적시켜 DOM에 중복 요소가 쌓인
    상태에서만 나타나는 문제**였고, 깨끗한 새 회차(행 1개)에서는 옵션 5개가 전부 정상
    렌더링됨을 확인 — 실사용(회차마다 새로 검색/추가) 시나리오에선 문제 없음.
  - `K2BClient.add_major_hazard_work()`를 발생형태/위험작업/시작일/종료일을 한 번에 전부
    설정하는 형태로 재작성(기존엔 위험작업 종류만 선택했음). 깨끗한 회차에서 2건 연속 추가
    →둘 다 전 필드 정확히 반영되는 것까지 end-to-end 검증(발생형태 낙하/질식, 위험작업
    2종, 예정시기 각각 다른 날짜).
  - `_scroll_into_view()`도 일반화: 기존엔 아래로만 스크롤했는데, 이 영역처럼 화면 한
    번에 안 걸치는 섹션에서는 대상이 이미 지나쳐 위(y<0)에 있는 경우도 있어 방향을
    자동으로 판단하도록 수정.
  - `worker.py`에 `d.major_hazard_works` 리스트를 순회하며 연결.

**6. 현장개선사진 연결** — 12-1의 `previous_finding.completion_photo_path`("이전지적사항"
조치완료 증빙사진)을 `db_reader.py`에서 조회해 `K2BSubmissionData.improvement_photo_paths`로
추가, `worker.py`에서 있으면 `attach_photos("현장개선", ...)` 자동 호출.

### 기타
- `K2BSubmissionData`에 `MajorHazardWork` 데이터클래스, `bad_site_notify*` 필드,
  `scaffold_types`(리스트로 일반화, 기존 `scaffold_type` 단일값에서 변경) 추가.
- `desktop/main_window.py`에 "K2B 전용 항목" 카드 신설 — 2/3/4/5번이 전부 이 카드에 모임.
- 앱을 실제로 띄워 새 UI 섹션이 정상 렌더링되는 것까지 화면으로 확인.

### 사용자 실사용 중 발견된 버그 2건 추가 수정 (비계사용현황)
사용자가 실제 워커(앱 → 자동입력)로 돌려보다가 "비계사용현황 '미사용'" 단계에서 두 차례
연달아 크래시 발생, 자동입력이 대형사고 위험작업까지 못 감. 디버그 브라우저로 직접
재현해서 원인 둘 다 찾아 고침.

**버그 A: "사용"/"미사용" 텍스트가 페이지에 2개씩 존재, `.first`가 엉뚱한 걸 골랐음.**
DOM에 "미사용" 텍스트가 2개 있는데 하나는 화면에 안 보이는 다른 요소(id 없음, y좌표가
한참 아래쪽)이고 실제 눈에 보이는 라디오는 **마지막(.last)** 매치였음. `.first`를 썼더니
안 보이는 엉뚱한 요소를 클릭 시도해서 "Element is outside of the viewport" 오류로
막힘(1차 시도 때는 Playwright 내부 버그로 다른 모양의 오류가 남 — `Cannot read
properties of undefined` — 인데 원인은 같은 strict-mode 다중매치 문제였음). `.first`→
`.last`로 수정.

**버그 B: 비계종류(강관비계/시스템비계) 체크박스는 기본 720px 뷰포트 밖에 있고, 스크롤로
넣을 방법이 없음.** `.last`로 라디오 자체는 고쳤는데 "사용" 선택 후 나타나는 비계종류
체크박스에서 또 "Element is outside of the viewport" 재발. DOM 조사 결과 이 체크박스는
상세모달이 아니라 뒤에 깔린 검색화면(103015010) 안의 `...ScrollableInnerContainerElement`
(scrollHeight=1252, clientHeight=677, `overflow:hidden`)에 속해 있는데, 이 컨테이너는
모달과 달리 마우스 휠에 전혀 반응하지 않고(`scrollTop`을 JS로 직접 바꿔봐도 시각적으로
안 움직임 -- Nexacro가 순수 자체 로직으로 자식 요소의 `top` 픽셀값을 재계산해서 배치하는
것으로 추정) 페이지 자체도 `scrollY`/`document.scrollHeight`가 뷰포트와 똑같아 브라우저
네이티브 스크롤도 안 됨. 이 체크박스의 실제 y좌표(988px)가 기본 뷰포트(720px)보다 커서
생기는 문제임을 확인.

해결: `open_browser()`에서 컨텍스트 뷰포트를 `{width:1280, height:1200}`으로 키움 --
Chrome의 뷰포트는 실제 OS 창 크기와 무관하게 CDP로 독립적으로 에뮬레이트되는 것을
확인했고(`page.set_viewport_size()`로 실측: 뷰포트를 키우자 스크롤 없이도 해당 체크박스가
바로 클릭 가능한 범위 안에 들어옴), 이렇게 하면 이 필드뿐 아니라 앞으로 비슷하게 검색화면
뒤쪽 깊은 곳에 있는 다른 필드도 스크롤 문제 없이 커버될 걸로 기대함. `open_browser()`로
실제 로그인부터 새 차수 추가, 비계사용현황 '사용'+강관비계 체크까지 전체 흐름을 처음부터
끝까지 재실행해 성공 확인.

(참고: 오늘 디버그 브라우저를 여러 번 재시작하면서 이전 프로세스를 제대로 안 죽이고
새로 띄우는 바람에 같은 포트(9333)에 좀비 크롬이 20개까지 쌓여 있었음 -- 이게 오후 한때
"인터넷이 꺼졌다"처럼 보인 원인. 실제 인터넷 연결 문제는 아니었고, 좀비 프로세스를 전부
정리하고 재실행해서 해결. `launch_debug_browser.py`를 다시 실행하기 전엔 항상 기존
프로세스가 완전히 죽었는지 확인할 것.)

### 뷰포트 확대가 대형사고 위험작업을 깨뜨림 -- 원인 규명 후 "임시 확대 후 즉시 원복" 방식으로 재수정
사용자가 실사용 중 새 오류 2건 신고: (1) 앱에서 대형사고 위험작업에 선택한 값이 실제 K2B에
반영 안 됨, 앱의 대형사고위험작업 드롭다운이 "선택" 플레이스홀더 없이 첫 번째 실제 옵션이
기본 선택된 상태로 보임 (2) 앱 실행 중 "Locator.click: Timeout 30000ms exceeded --
waiting for ...controlcombo].last" 오류와 함께 크롬 창이 꺼짐.

**(1) 수정**: `desktop/main_window.py`의 `_add_hazard_row()`에서 대형사고위험작업
콤보박스에 `_NOT_SELECTED`("(선택 안 함)")를 첫 항목으로 추가(K2B의 "선택" 플레이스홀더와
동일한 의도). `_apply_manual_fields()`에서 이 값이 그대로 남아있는 행은 자동입력에서
건너뛰고 로그로 경고하도록 함(엉뚱한 기본값이 조용히 전송되는 걸 방지).

**(2) 원인 규명**: 어제(Day2 앞부분) 비계종류 체크박스 문제를 고치면서 `open_browser()`의
컨텍스트 뷰포트를 1280x1200으로 통째로 키워뒀었는데, 이게 대형사고 위험작업 그리드를
깨뜨리고 있었음. `open_browser()` 경로로 직접 재현해서 확인: 뷰포트를 키운 상태로
발생형태 콤보 셀을 클릭하면(y=1097, 커진 뷰포트 안에 있는데도) 셀은 클릭되는데 공용 콤보
오버레이 자체가 아예 안 생김(`combo count: 0`). Nexacro가 "화면에 보이는 범위"를 실제
브라우저 뷰포트가 아니라 내부적으로 기본 720px 기준으로 처리하는 것으로 보이며, 뷰포트를
그 이상으로 키워두면(디바이스 메트릭 오버라이드든 `no_viewport=True`로 실제 창을 키우든
동일하게) 이 그리드의 바인딩이 깨짐을 확인함(둘 다 재현/검증함).

**해결(1차)**: `open_browser()`의 뷰포트 확대는 완전히 되돌리고(기본 1280x720 유지),
비계종류 체크박스만 클릭하는 그 찰나에만 `page.set_viewport_size()`로 임시로
키웠다가(1280x1200) 클릭 직후 바로 원래 크기로 되돌리는 방식
(`K2BClient._click_scaffold_type_checkboxes()` 신규)으로 재수정. 대형사고 위험작업
2건 연속 추가는 이 방식으로 안정적으로 재검증됨(뷰포트가 정확히 1280x720으로 복귀하는
것도 로그로 확인, 이 부분은 CONFIRMED로 문제 없음).

### 비계종류(강관비계/시스템비계) 체크박스 -- 진짜 원인 발견, 완전히 해결(CONFIRMED)
그런데 위 "해결(1차)" 직후 재검증 과정에서, 비계종류 체크박스 자체의 클릭 성공 여부가
**들쭉날쭉함**을 발견함(사용자가 "강관비계로 체크했는데 시스템비계만 반영됐다"고 신고 →
재현 확인). 여러 차례 격리 테스트(단독 클릭/연속 클릭/2번 클릭/실제 좌표 클릭/사람처럼
느린 클릭/키보드 focus+Space/`no_viewport=True`로 진짜 창 키우기)를 했는데 전부 실패.

**결정적 단서**: 사용자가 뷰포트를 키운 상태의 브라우저 창에서 **직접 마우스로 클릭**했더니
정상적으로 체크됨. 셀렉터/좌표는 정확한데 "진짜" 클릭만 통하고 Playwright의 클릭은 안
먹힌다는 게 실마리였음.

**진짜 원인 발견**: `document.elementFromPoint()`로 실제 클릭 좌표에 무엇이 있는지 확인한
결과, 그 좌표 위에 **z-index 1000002짜리 반투명 회색 모달 배경 딤(dim) 오버레이**
(`background-color: rgba(102,102,102,0.298)`, 모달을 열 때 뒷배경을 어둡게 처리하는
레이어)가 덮여 있었음. 그리고 페이지에서 "강관비계" 텍스트를 전체 검색해보니 **완전히
동일한 문구가 실제로 2곳에 존재**:
1. `..._103015010_div_Work_CURR_SCFDG_INSTL_STLE_CMMN_CD_5` (id에 "CURR_" 있음) --
   Day1부터 계속 이 id를 써왔는데, 화면 좌표가 **모달 창 범위(x=280~1000) 밖**(x=1120)에
   있어서 위 딤 오버레이에 항상 가려져 있었음(모달이 열려있는 한 절대 클릭 불가능한
   자리). 검색화면(103015010) 소속이라는 Day1 기록 자체는 맞았지만, 그 요소가 **화면에
   실제로 보이는 진짜 체크박스가 아니었다**는 게 몰랐던 사실.
2. `..._EBP00130_P01_form_tab_Main_tabpage1_SCFDG_INSTL_STLE_CMMN_CD_5` (id에 "CURR_"
   없음, **상세모달 소속**) -- 화면 좌표가 모달 범위 **안**(x=797)이라 실제로 보이고
   클릭도 되는 진짜 체크박스.

즉 지금까지 자동화가 가리키던 요소와 사람 눈에 보이는(그래서 사용자가 직접 클릭해 성공한)
요소가 **애초에 서로 다른 요소**였다 -- Playwright가 아무리 정확하게 좌표를 계산해도
그 자리엔 항상 딤 오버레이만 있었으니 클릭이 씹힐 수밖에 없었고, 어쩌다 하나가 반영된
것처럼 보였던 건 순전히 이전 시도들의 우연한 부작용(뷰포트 리사이즈 타이밍 등)이었을 뿐.

**해결**: `k2b_selectors.py`의 `SCAFFOLD_TYPE_CHECKBOX_IDS`를 모달 소속의 올바른 id로
교체(`_modal()` 접미사 매칭 사용, "CURR_" 없는 쪽). `k2b_client.py`의
`set_scaffold_usage()`에서 뷰포트 임시 확대 같은 우회 로직을 전부 제거하고, 다른 모달
체크박스(통보방법 등)와 동일하게 평범한 `_scroll_into_view()` + `click()`만으로 처리하도록
단순화. `open_browser()`의 뷰포트도 항상 기본값(1280x720)으로 고정.

실측 검증: 비계사용현황 '사용' + 강관비계 + 시스템비계 둘 다 체크 → 대형사고 위험작업
2건 연속 추가까지 `open_browser()` 실제 경로로 처음부터 끝까지 재실행해 전부 정상 반영
확인(뷰포트 조작 전혀 없이, 다른 필드들과 동일한 방식으로 안정적으로 동작함).

**교훈**: 화면에 같은 텍스트/같은 필드명이 있어도, 그 뒤에 완전히 다른(모달 소속 vs
검색화면 소속) DOM 요소가 우연히 겹쳐 존재할 수 있다 -- 셀렉터로 요소를 찾았을 때
"찾아졌다"는 사실만으로 안심하지 말고, 그 요소가 **실제로 사람 눈에 보이는 그 요소가
맞는지** `document.elementFromPoint()`로 교차 확인하는 습관이 필요함(이번 건은 화면
겉보기로는 완전히 똑같아 보여서 각별히 헷갈렸음).

---

## 2026-09-16 (Day 2 이어서) — 대형사고 위험작업: 발생형태별 드롭다운 필터링 발견/반영

사용자가 K2B 사이트를 직접 조작하며 이전엔 몰랐던 사실을 발견함: "대형사고 위험작업"
드롭다운의 선택지는 고정된 5개 목록이 아니라, **같은 행의 "발생형태"에서 무엇을
골랐는지에 따라 동적으로 필터링**된다. 스크린샷 5장(전체/붕괴/도괴/낙하/질식)으로
각 발생형태별 실제 노출 항목을 확인:

| 발생형태 | 대형사고 위험작업 (노출되는 항목) |
|---|---|
| 전체 | 5개 전부 |
| 붕괴 | 비계 설치 및 해체 |
| 도괴 | 거푸집동바리(작업발판 일체형 거푸집 포함) 설치 및 해체 / 흙막이지보공(내공단면적 2㎡미만 터널 지보공 포함) 설치 및 해체 |
| 낙하 | 기타 추락위험 장소·작업(달비계 작업, 지붕공사 등) |
| 질식 | 밀폐공간 및 화재·폭발 |

**반영 내용**:
- `core/k2b_selectors.py`: `MAJOR_HAZARD_WORK_OPTIONS_BY_OCCURRENCE` 딕셔너리 신규 추가
  (발생형태 → 위험작업 목록 매핑, CONFIRMED).
- `core/k2b_client.py`의 `add_major_hazard_work()`: 검증 로직을 고정 전체 목록 체크에서
  "해당 발생형태에서 유효한 목록에 속하는지" 체크로 강화. 잘못된 조합이면
  `ValueError`로 명확히 실패하도록 함(사이트에 없는 조합을 억지로 넣으려다 자동화가
  알 수 없는 이유로 멈추는 것 방지).
- `desktop/main_window.py`: `_add_hazard_row()`에서 발생형태 콤보(`occurrence_combo`)의
  `currentTextChanged`를 위험작업 콤보(`work_combo`) 갱신 핸들러(`_refresh_hazard_work_options`)에
  연결. 발생형태를 바꿀 때마다 위험작업 콤보를 지우고 "(선택 안 함)" 플레이스홀더 +
  해당 발생형태의 필터링된 옵션만 다시 채움 → 항상 사이트와 동일하게 "선택"부터
  시작하고, 유효하지 않은 조합이 남아있지 않도록 보장.

이걸로 앱 UI가 사이트의 실제 동적 필터링 동작과 완전히 일치하게 됨.

---

## 2026-09-16 (Day 2 이어서 #2) — 대형사고 위험작업 자동화 실사용 크래시: "흙막이지보공" 문자열 오타

사용자가 실제 앱으로 도괴+흙막이지보공 조합을 시도하다가 "대형사고 위험작업 종류" 드롭다운을
연 채로 그대로 멈추고 브라우저가 꺼지는 크래시 발생. 오류창: `Locator.click: Timeout
30000ms exceeded ... waiting for get_by_text("흙막이지보공(내공단면적 2㎡미만 터널 지보공
포함) 설치 및 해체", exact=True).last`.

**진단 과정**: 스크린샷상으로는 드롭다운에 목표 옵션이 멀쩡히 보이고 있어서 클릭 자체가
막힌 줄 알았으나, 30초 내내 열려있는데도 못 찾는다는 건 클릭 방해가 아니라 "그 정확한
텍스트를 가진 요소가 아예 없다"는 뜻 → 디버그 스크립트(`scratchpad/debug_hazard_dropdown*.py`,
1회성)로 실제 K2B 계정 로그인 후 도괴 선택 → 드롭다운 오픈 → 옵션 텍스트를
`textContent`+코드포인트(`codePointAt`) 단위로 덤프해 코드에 저장된 문자열과 직접 대조.

**원인**: 이전 세션에서 사용자가 준 스크린샷을 보고 손으로 옮겨 적은
`MAJOR_HAZARD_WORK_OPTIONS`의 "흙막이지보공(내공단면적 ...)" 문자열에서 "내공"과
"단면적" 사이 띄어쓰기가 빠져 있었음(실제 사이트 텍스트는 "내공 단면적", 공백 있음).
`get_by_text(..., exact=True)`는 공백 하나 차이도 다른 문자열로 취급해 영원히 못 찾고
30초 타임아웃 → 예외 발생 → `open_browser()`의 `with` 블록이 브라우저를 닫아버려 "멈추고
꺼짐"으로 체감됨.

**검증**: 같은 디버그 방식으로 전체/붕괴/도괴/낙하/질식 5개 발생형태를 전부 순회하며
드롭다운에 실제 노출되는 옵션들을 전부 코드포인트 단위로 재확인 — 이 문자열 하나만
틀렸고, 나머지 4개 옵션 문자열과 발생형태별 필터링 매핑(`MAJOR_HAZARD_WORK_OPTIONS_BY_
OCCURRENCE`)은 전부 실제 사이트와 정확히 일치함을 CONFIRMED.

**수정**: `core/k2b_selectors.py`에서 해당 문자열에 공백 추가(`"내공단면적"` →
`"내공 단면적"`, `MAJOR_HAZARD_WORK_OPTIONS`와 `MAJOR_HAZARD_WORK_OPTIONS_BY_OCCURRENCE["도괴"]`
양쪽 다). 수정 후 `client.add_major_hazard_work("도괴", "흙막이지보공...", ...)`를 실제
K2B 계정으로 처음부터 다시 실행해 발생형태 선택 → 위험작업 선택 → 시작일/종료일 설정까지
예외 없이 전부 성공하는 것까지 재확인함.

**교훈**: 스크린샷을 보고 손으로 옮겨 적은 한글 문자열(특히 길고 괄호·단위기호가 섞인
텍스트)은 `exact=True` 매칭에 쓰기 전에 실제 DOM의 `textContent`를 코드포인트 단위로
대조해볼 것 — 사람 눈에는 공백 하나 빠진 게 스크린샷에서 거의 안 보인다. 이 방식(로그인 →
목표 화면 도달 → `page.evaluate`로 후보 요소들의 텍스트+코드포인트 덤프 → 코드 문자열과
diff)을 앞으로 비슷한 "긴 한글 문자열 정확히 일치시켜야 하는" 필드에도 재사용할 것.

---

## 2026-09-16 (Day 2 이어서 #3) — 현장 검색: 띄어쓰기 차이/여러 건 검색결과 안정성 개선

사용자가 "검색결과가 여러 건일 때 행 선택 안정성 재검증 필요" TODO를 다시 짚으며, 12-1과
K2B에 등록된 현장명 띄어쓰기가 서로 다를 수 있다는 우려를 제기. 실측으로 다음을 확인:

- **K2B 검색은 부분(substring) 일치는 지원하지만 띄어쓰기엔 전혀 관대하지 않음**
  (CONFIRMED): 원래 이름에서 공백 하나만 추가/제거/이동해도 검색결과 0건.
- **부분 문자열로도 정상 검색됨**(CONFIRMED): 전체 이름 대신 첫 단어("태봉근린공원")나
  짧은 접두어("태봉")만 넣어도 같은 결과가 뜸.
- 실제로 "태봉"으로 검색하니 서로 다른 현장 3건("태봉근린공원 조성사업(2단계) 토목공사",
  "태봉초 급식실 환기설비 개선 기계설비공사", 주소 칸의 "...태봉로 216 일원")이 동시에
  뜨는 실제 사례를 발견 — 정확히 사용자가 우려하던 "검색결과 여러 건" 상황.

**수정 내용** (`core/k2b_client.py`):
- `search_site()`: 검색어로 전체 이름 대신 **첫 단어(공백 앞부분)만** 사용 → 이름 중간의
  띄어쓰기 차이가 검색 자체를 막지 못하게 함.
- `select_result_row()`: `get_by_text(exact=True)` 대신 **양쪽 다 공백을 전부 제거하고
  비교**하는 방식으로 교체(`page.evaluate`로 DOM을 스캔해 정규화된 텍스트가 정확히
  일치하는 leaf 요소를 찾음). 후보가 0개면 "못 찾음" 예외, 2개 이상이면(K2B에 완전히
  동일한 이름이 중복 등록된 경우) "여러 건이라 자동으로 못 고름 -- 실제 텍스트 목록"을
  담은 예외를 던져 사람이 판단하게 함. 공백만 무시하고 다른 글자 차이(예: "조성사업" vs
  "조선사업")는 여전히 구분됨을 실측 확인 -- 진짜 같은 현장만 관대하게 봐주고 비슷해
  보이는 다른 현장을 잘못 고르지 않음.

**버그 하나 발견/수정**: 처음 구현한 `page.evaluate()` 기반 매칭은 `get_by_text().click()`과
달리 Playwright의 자동 대기(actionability auto-wait)가 적용되지 않아서, 검색 버튼 클릭
직후 결과 그리드가 아직 갱신되기 전에 검사하면 0건으로 오판하는 레이스 컨디션이 실제로
재현됨(실측). 최대 10초(0.5초 x 20회) 재시도 루프를 추가해 해결.

**검증**: (1) 정상 케이스(실제 이름 그대로) 검색→선택→상세보기 성공, (2) 일부러 띄어쓰기만
다르게 꼬아 만든 가짜 이름으로 `select_result_row`를 호출해도 정상적으로 같은 행을 찾아
클릭하고 상세보기까지 진입 성공, (3) 기존 대형사고 위험작업 흐름도 회귀 없이 정상 동작
재확인. 전부 `open_browser()` 실제 경로로 실계정 로그인 후 확인함(CONFIRMED).

**향후 논의(코드 미반영)**: 사용자가 이름 뒤에 "-2026-0001" 같은 고유번호를 붙이는 방안을
제안했으나, 이건 K2B 쪽 현장명에도 사람이 똑같이 입력해줘야 효과가 있는 컨벤션이라
근본 해법은 아님. 대신 `core/db_reader.py`의 `management_no`(K2B가 1회차 저장 후 자동
부여하는 고유 관리번호)로 매칭하는 게 더 근본적인 해법일 수 있음 -- 2회차 이후엔 이름
대신 이 번호로 검색/선택하고, 이름 기반 검색은 관리번호가 없는 "진짜 1회차 신규 등록"에서만
필요해지는 구조. TODO 1번(1회차 신규 등록 기능)과 함께 나중에 고려하기로 함.

---

## 2026-09-16 (Day 2 마무리) — 코드 리팩토링(600줄 제한) + 지도건수 등 항목 보류 처리

**리팩토링**: `core/k2b_client.py`가 613줄로 600줄을 넘어서 기능별 mixin 파일로 분리:
- `core/k2b_client_base.py` — 공통 상태(`self.page`/`self.log`)와 헬퍼(`_scroll_into_view`/
  `_type_into`/`_pick_date_in_open_calendar`), `ReportModificationExpiredError`
- `core/k2b_client_nav.py` — 로그인/메뉴이동/검색/행선택/상세보기/차수추가
- `core/k2b_client_fields.py` — 기술지도일/공정률/현장책임자/특이사항/비계사용현황/
  현재작업공정/통보방법/이전 기술지도 이행여부
- `core/k2b_client_hazard.py` — 대형사고 위험작업 그리드
- `core/k2b_client_attachments.py` — 불량사업장 통보, 사진/보고서 파일 첨부
- `core/k2b_client.py` — 위 mixin들을 전부 상속하는 `K2BClient` + `open_browser()`만 남김.
  `from core.k2b_client import K2BClient, open_browser, ReportModificationExpiredError`
  같은 기존 임포트 경로는 전부 그대로 동작(재수출). 실제 로직/셀렉터/실기록 주석은
  변경 없이 그대로 옮김.

검증: mixin 합성 후 `K2BClient`에 모든 메서드가 정상적으로 존재하는지 MRO까지 확인,
실제 계정으로 로그인→검색→선택→상세보기→대형사고위험작업 추가 흐름과 띄어쓰기 무시
행선택 흐름 둘 다 리팩토링 후 재실행해 회귀 없음 확인(CONFIRMED).

**지도건수/경영책임자·건설업체 통보일/발주자 통보일자**: 어제는 "굳이 자동화 안 해도
됨"으로 정리했었으나, 사용자가 보고서를 다시 보니 (1) 이미 12-1에서 읽어오고 있는
"교육인원"처럼 이 항목들도 실제 보고서 데이터에 대응될 수 있고 (2) 지도건수 = 지적사항
건수일 가능성, (3) 배포자료건수 = 보고서 11번 항목(제공자료) 개수일 가능성을 제기함.
아직 미확정 -- **고객 미팅 후 결정 예정**(자동화 대상 아님이 아니라 보류 상태로 재조정).

**추가 발견**: "교육인원"은 이미 12-1 DB(`safety_education.attendee_count`)에서 읽어와
미리보기에는 표시되고 있는데, 실제 K2B 화면에 채워 넣는 자동화 코드는 아직 없음
(`EDUCATION_COUNT_INPUT_ID` 셀렉터만 확인, 입력 함수 미작성) -- 지도건수 등을 처리할 때
같이 정리하기로 함(아직 미착수).

실제 K2B 화면에서 이 필드들 위치를 스크린샷으로 확인함: "지도건수"는 공정률 옆(단위
"건", 옆에 "배포자료건수"도 있음), "경영책임자(건설업체 본사) 통보일"은 분기(1~4분기)
체크 + 날짜 하나짜리 단일 필드(원래 별개 필드로 착각했었음), "건설공사 발주자
통보일자"는 그 아래 별도 행의 날짜 필드.

## 2026-09-17 (Day 3) — 배포용 DB 경로 해결 + "지적사항 사진" K2B 현장점검사진 매핑

### 1. exe 배포 시 12-1 실제 DB를 못 찾던 문제 발견/수정
사용자가 "배포할 때 모든 사람이 다 오류 없이 사진 데이터를 다룰 수 있게 해야 한다"고
요청 → 확인해보니 `core/config.py`의 frozen(exe) 분기가 **항상 exe에 번들된 데모용
`data/app.db`만** 바라보고 있었음(12-1이 exe로 돌 때 자기 exe 폴더 밑에 실제
`data/app.db`를 새로 만드는 것과 무관하게). 이대로 배포하면 담당요원이 12-1로 실제
보고서를 아무리 써도 14는 예전 데모 데이터만 계속 보게 되는 배포 차단급 버그였음.

사용자와 논의 후 배포 모델 확정: 12-1/14 둘 다 데스크톱 프로그램으로 담당요원 각자
PC에 배포하고("보고서 작성→K2B 제출"을 한 흐름으로 사용, 나중에 잘 되면 13번 프로젝트
그룹웨어로 이식 예정), **두 exe를 항상 같은 상위 폴더 밑에 정해진 이름으로 나란히
설치**하는 규칙으로 가기로 함. `core/config.py`를 수정: frozen일 때 자기 exe 폴더의
부모 밑에서 12-1의 dist 폴더 이름(`한국미래안전_기술지도결과보고서`, 12-1
`packaging/build_exe.py`의 `APP_NAME`과 반드시 일치)을 찾아 그 실제 `app.db`를
쓰도록 변경. 그 폴더가 없으면(12-1 없이 14만 영업용 데모로 보여줄 때) 기존처럼
exe 번들 데모 DB로 폴백. README에 "배포(담당요원 PC) — 폴더 배치 규칙" 섹션 추가.

### 2. "지적사항 사진"이 K2B에 안 올라가던 문제
사용자가 실제 K2B 저장된 차수 화면을 보여주며 지적: "현장점검 사진" 칸에 이미
"조도사진"/"지도사진1,2" 같은, 단순 점검사진이 아닌 파일들이 올라가 있는 걸 확인 →
K2B는 애초에 현장전경/현장점검/현장개선 3칸뿐이고 "지적사항 전용" 칸이 없어서,
실무에서는 그 외 모든 현장 방문 사진이 "현장점검 사진" 칸에 뭉쳐서 올라간다는 뜻이었음.

사용자가 정확한 매핑을 직접 불러줌(마법사 단계 기준):
- 마법사3 전경사진 → K2B 현장전경사진 (기존 그대로)
- 마법사3 점검사진 → K2B 현장점검사진 (기존 그대로)
- 마법사4 "이전지적사항"의 원본 사진(`previous_finding.photo_path`, 이행결과 증빙사진인
  `completion_photo_path`와는 다른 컬럼) → K2B 현장점검사진 (신규)
- 마법사4 "이행결과 사진"(`previous_finding.completion_photo_path`) → K2B 현장개선사진
  (기존 그대로, 변경 없음)
- 마법사8 "지적사항" 사진(`finding.photo_path`, 이번 회차 새 지적사항, 최대 4건) →
  K2B 현장점검사진 (신규)
- 마법사10-1 TBM 교육사진(`safety_education.photo_path`) → K2B 현장점검사진 (신규)
- 마법사10-2 계측기 사진(`measurement.photo_path`) → K2B 현장점검사진 (신규, 조도계/
  가스농도측정기가 주로 쓰이지만 향후 편의를 위해 7종 계측기 전부 포함하기로 함)
- 슬롯이 여러 개인 항목(이전지적사항/지적사항 최대 4건)은 사진이 채워진 것 전부 포함

`core/db_reader.py`의 `inspection_photo_paths`를 위 5개 SELECT(점검사진 + 이전지적사항
원본사진 + 지적사항사진 + TBM교육사진 + 계측기사진)를 합친 리스트로 변경. `worker.py`는
이미 `client.attach_photos("현장점검", d.inspection_photo_paths)`를 그대로 호출하고
있어서 호출부 수정은 불필요. 실제 12-1 개발 DB로 `get_submission_data()`를 직접 호출해
merge 전 점검사진 2건 → merge 후 6건(추가된 계측기 사진 등 포함)으로 정상 반영됨을
확인.

### 3. 실제 K2B 화면 end-to-end 검증 + 새 제약 발견/수정: "5건 이상 업로드 불가"
디버그 브라우저(포트 9333)로 사용자가 로그인 후 임의 현장 상세보기까지 진입 → Claude가
CDP로 그 브라우저에 붙어서 "+추가"로 **미저장 테스트 차수(차수 4, 기존 1~3차와 별개)**를
만든 뒤 위에서 merge된 6장을 `attach_photos("현장점검", ...)`로 시도.

**1차 시도 실패**: "5건 이상의 파일 업로드는 허용되지 않습니다" 팝업으로 전체 거부됨
(부분첨부 아니고 전체 취소) — K2B의 파일선택창 자체가 한 번에 최대 4개까지만 허용하는
제약이 실제로 있음을 처음 확인함(CONFIRMED). 여러 소스를 합치면서 4장을 넘는 경우가
실제로 생겨 이번에 처음 드러남 — 지금까지 몇 장 안 되는 사진으로만 테스트해서 안 걸렸던
제약.

`core/k2b_client_attachments.py`의 `attach_photos()`를 수정: 4장씩 나눠서 "사진첨부"
버튼을 여러 번 눌러 첨부하도록 배치 처리 추가(`_MAX_FILES_PER_ATTACH = 4`).

**재검증**: 같은 테스트 차수에서 재시도 → "파일 전송 상태" 진행률 팝업(조도사진.jpg,
가스사진.jpg 등)까지 실제로 뜨며 정상 전송, 완료 후 현장점검 사진 표에 6행 전부(교육사진/
조도사진/가스사진 등 새로 합쳐진 사진 포함) 정상 표시됨을 스크린샷으로 확인(CONFIRMED,
실제 파일 업로드 end-to-end).

**정리**: 테스트가 끝난 뒤 저장 버튼을 누르지 않고 "닫기"만 눌러 모달을 닫음 — "+추가"로
만든 차수는 저장 전까지 서버에 반영 안 되므로 기존 1~3차 실제 데이터는 전혀 건드리지
않고 안전하게 정리됨(닫기 직전 차수 필드가 "4"이고 공정률/지도건수 등 전부 빈 값인 걸
DOM에서 직접 읽어 재확인함 -- 기존 차수를 실수로 덮어쓴 게 아님을 이중 확인).

**교훈**: "닫기" 버튼 텍스트가 같은 요소가 여러 개(`_form_btn_CloseTextBoxElement`와
`_form_btn_Close00TextBoxElement`) DOM에 겹쳐 있고, 실제 클릭 이벤트를 받는 건 후자
쪽이었음(전자를 클릭하면 "element intercepts pointer events"로 계속 실패) -- 비계종류
체크박스 때와 비슷하게, 겉보기 텍스트만으로 요소를 특정하면 안 되고 실제로 이벤트를
받는 쪽인지 확인 필요.

### 4. 현장전경사진 테스트 중 실사용 현장 오염 위험 사고 -- 중요, 재발 방지 필요

앞의 검증에 이어 현장전경사진(overview)도 첨부 테스트를 하려다 심각한 혼란이 발생함.
`db_reader.get_submission_data`를 부를 때 **현장명을 사용자에게 확인하지 않고
`list_sites()[0]`(12-1 DB에서 가장 최근 추가된 현장)을 임의로 골라 자동화 스크립트를
돌림** -- 이게 사고의 시작이었음. 이 이름으로 `search_site()`/`select_result_row()`를
불러도 화면에 계속 **차수3/공정률5/기술지도일 2026-08-26(사용자가 실사용 중인 진짜
현장의 실제 데이터로 추정)**이 떴는데, 스크립트가 "차수4 추가 완료", "차수5 추가 완료"를
계속 성공 보고해서 한동안 "내가 새 빈 차수를 테스트용으로 만든 것"이라고 오판했음.
사용자가 실제 화면에서 "5차수까지 보인다"고 확인해줬고, 이게 빈 테스트 데이터가 아니라
**공정률이 회차마다 실제로 증가하는 진짜 데이터**임을 사용자가 직접 확인 -- 삭제 시도
직전에 사용자가 "건들지마"로 중단시켜서 실사용 데이터 삭제 사고는 막음(CONFIRMED,
아무것도 삭제하지 않음). 브라우저를 완전히 새로 띄워도 같은 차수3 데이터가 계속
나타나는 것으로 보아, 이 세션 내내 다루고 있던 게 12-1의 "아름드리아파트"(가상 테스트
데이터 이름)가 아니라 **사용자가 실제로 상시 사용하는 현장(아마 "태봉근린공원
조성사업(2단계)")**이었을 가능성이 높음 -- 정확한 원인(왜 그 이름으로 검색했는데 계속
그 현장이 뜨는지)은 끝내 명확히 못 밝힘.

**재발 방지 원칙(반드시 지킬 것)**:
1. **라이브 K2B 화면에서 어떤 자동화 테스트를 하든, 현장명은 반드시 사용자에게 먼저
   확인받고 진행한다** -- `list_sites()[0]` 같은 임의 선택 절대 금지.
2. **"+추가"가 저장 버튼 없이도 실제로 서버에 반영될 수 있다**는 가능성을 확인함(기존
   "저장 전까지는 반영 안 됨" 가정이 최소 사진첨부 이후엔 안 맞을 수 있음) -- 앞으로
   라이브 테스트에서 "+추가"를 누르는 순간부터 이미 되돌리기 어려운 작업일 수 있다고
   가정하고 신중히 접근할 것.
3. **테스트 전용 현장을 사용자가 지정해준 경우에만(예: 태봉근린공원, 회차 수를
   사용자가 정확히 알고 있는 곳) 그 현장 이름으로 명시적으로 테스트할 것**, 12-1
   DB의 아무 현장이나 골라 쓰지 말 것.
4. 화면 스크롤/모달 close 자동화가 이 세션에서 여러 번 불안정하게 실패함(스크롤 위치에
   따라 요소가 화면 밖에 있어 클릭 실패, 모달이 완전히 안 닫히고 겹쳐서 이전 데이터가
   계속 보이는 현상 등) -- 라이브 검증이 필요하면 사용자가 직접 화면을 조작하고
   Claude는 결과만 스크린샷/DOM으로 확인하는 쪽이 더 안전함.
